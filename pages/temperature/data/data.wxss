page {
  color: #232323;
  background: #f2f2f2;
  font-size: 30rpx;
}
.container .item {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  padding: 0 30rpx;
  margin: 0 0 20rpx;
  line-height: 100rpx;
  background: #fff;
  align-items: center;
}
.title {
  line-height: 100rpx;
  padding: 0 30rpx;
}
.container .btn {
  margin: 100rpx 30rpx;
}
.result .item {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  padding: 0 30rpx;
  margin: 0 0 20rpx;
  line-height: 100rpx;
  background: #fff;
}
.result .btn {
  margin: 100rpx 30rpx;
}
.result .btn button:nth-child(1) {
  margin: 0 0 30rpx;
}
progress {
  width: 450rpx;
}


/* 默认背景样式*/
radio .wx-radio-input{
  width: 40rpx;
  height: 40rpx;
  display: none;
}

/* 选中后的 背景样式 （红色背景 无边框 可根据UI需求自己修改） */
radio .wx-radio-input.wx-radio-input-checked{
  width: 40rpx; /* 选中后对勾大小，不要超过背景的尺寸 */
  height: 40rpx; /* 选中后对勾大小，不要超过背景的尺寸 */
  border: none;
  background: #E1C371;
}
/* 选中后的 对勾样式 （白色对勾 可根据UI需求自己修改） */
radio .wx-radio-input.wx-radio-input-checked::before{
  border-radius: 50%;/* 圆角 */
  width: 40rpx; /* 选中后对勾大小，不要超过背景的尺寸 */
  height: 40rpx; /* 选中后对勾大小，不要超过背景的尺寸 */
  line-height: 40rpx;
  text-align: center;
  font-size:30rpx; /* 对勾大小 30rpx */
  color:#fff; /* 对勾颜色 白色 */
  border: 1rpx solid #E1C371;
  background: #E1C371;
  transform:translate(-50%, -50%) scale(1);
  -webkit-transform:translate(-50%, -50%) scale(1);
}

.radio {
  display: none;
}
.radio-group label {
  padding: 6rpx 16rpx;
  margin: 0 5rpx;
  border-radius: 20rpx;
  font-size: 20rpx;
}
.label_active {
  color: #fff;
  background: rgb(7, 193, 96);
}
.templete {
  display: flex;
  flex-direction: row;
  align-items: center;
}
.templete input {
  width: 150rpx;
  text-align: center;
}


.menu-list {
  position: relative;
  margin: 22rpx;
}
.menu-item {
  margin: 0  auto 12rpx;
  background: #fff;
  border-radius: 15rpx;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  padding: 35rpx 40rpx;
  align-items: center;
  box-shadow: 0 0 20rpx 1rpx rgba(0, 0, 0, 0.1);
}
.item-left image {
  width: 36rpx;
  height: 36rpx;
  margin: 0 30rpx 0 0;
  vertical-align: middle;
}
.item-right image {
  width: 20rpx;
  height: 32rpx;
  vertical-align: middle;
}


canvas {
  background: -webkit-linear-gradient(top, rgba(23, 110, 197, 0.9) 0%, rgba(33, 189, 246, 1) 100%);
  background-color: linear-gradient(top, rgba(23, 110, 197, 0.9) 0%, rgba(33, 189, 246, 1) 100%);
}



.modal .content {
  display: flex;
  justify-content: center;
  width: 80%;
  height: 170rpx;
  background: #fff;
  margin: 0 auto;
  border-radius: 15rpx;
}

.form{
  width: 80%;
  background: #fff;
  margin: 0 auto;
  border-radius: 15rpx;
}
.form .form-item {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  margin:40rpx 22rpx 0;
  position: relative;
  background: #fff;
  height: 106rpx;
  line-height: 106rpx;
  border-radius: 15rpx;
  border: 1rpx solid #f2f2f2;
  overflow: hidden;
}
.form .form-item label {
  position: absolute;
  left: 30rpx;
  top: 0;
  font-size: 32rpx;
}
.form .form-item input{
  padding: 0 40rpx 0 150rpx;
  font-size: 28rpx;
}
.form .form-item button {
  width: 200rpx;
  font-size: 28rpx;
  line-height: 4;
  margin: 0;
}
.form .form-item button::after {
  border: none;
}

.form .form-submit image{
  width: 517rpx;
  height: 138rpx;
  display: block;
  margin: 20rpx auto 0;
}
.submit{
  /* width: 480rpx; */
  margin: 50rpx 100rpx;
  background:#82C6D3 !important;
  border-radius:55rpx;
  color:#fff !important;
}
.submit::after {
  border: none;
}
.submit-disabled {
  background:rgba(130,198,211,0.4) !important;
}
.modal {
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  background: rgba(0,0,0,0.4);
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
}